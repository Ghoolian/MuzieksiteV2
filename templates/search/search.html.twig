{% extends 'base.html.twig' %}

{% block title %}Search result{% endblock %}

{% block body %}
    <h1>Search results</h1>

    <table class="table">

        <tr>
            <th>Id</th>
            <th>Naam</th>
            <th>Duration</th>
            <th>Album</th>
            <th>Artiest</th>

        </tr>
        <tbody>
        <tr>
            {% for results %}
        <td>{{ number.id }}</td>
        <td>{{ number.name }}</td>
        <td>{{ number.duration }}</td>
        <td>{{ number.album.name }}</td>
        <td>{{ number.album.artist.naam }} {{ number.album.artist.achternaam }}</td>
            {% endfor %}
        </tr>
        </tbody>
    </table>

    <a href="{{ path('home') }}">Back to home</a>



{% endblock %}